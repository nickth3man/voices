# Core dependencies
numpy>=1.20.0
torch>=1.10.0
psutil>=5.9.0
pyyaml>=6.0.0
soundfile>=0.12.1
librosa>=0.10.0
matplotlib>=3.7.0
pandas>=1.3.0

# Audio processing
cffi>=1.15.0
pycparser>=2.21

# Pre-installed dependencies (already working)
# These are already installed and working, so we pin them to avoid conflicts
numba==0.56.4
llvmlite==0.39.1

# Additional dependencies that might be needed
# scikit-learn>=1.0.0
# tqdm>=4.65.0
# scipy>=1.10.0